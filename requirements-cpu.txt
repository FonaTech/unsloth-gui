# ================================================================
# Unsloth GUI - CPU 兜底依赖（无 GPU，极慢，仅用于调试）
# ================================================================

# GUI 框架
gradio>=6.0.0

# 机器学习核心
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.28.0
peft>=0.10.0
trl>=0.8.6

# 量化（CPU 可用，但 4bit 加载在 CPU 下效果有限）
bitsandbytes>=0.43.0

# 图表
plotly>=5.0.0

# 贝叶斯超参数优化（自动调参 Tab）
optuna>=3.6.0

# 其他工具
packaging
sentencepiece
protobuf
